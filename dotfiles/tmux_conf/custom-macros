custom=(
'cd /var/log/volumez/ :logs'
'rm -rf /var/log/volumez/*; /root/go/bin/dlv exec --api-version=2 --listen=:2345 --headless --log ~/ -- --test.v: debugtest'
'rm -rf /var/log/volumez/*; mdadm -S /dev/md_*; dmsetup remove_all; modprobe -r brd; docker stop dynamock; docker rm dynamock; : cleantest'
'dmsetup ls --tree -oblkdevname: lsdm'
'watch -n0.5 cat /proc/mdstat: mdstat'
)

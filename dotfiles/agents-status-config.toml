[notification]
command = "$HOME/agents-status/core/notify-macos"
# Requires: brew install vjeantet/tap/alerter

[post-event]
commands = [
    "$AGENTS_STATUS_DIR/../tmux/scripts/refresh_dim_colors.sh",
]

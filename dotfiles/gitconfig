[user]
	email = ofirgal753@gmail.com
	name = Ofir Gal
[alias]
	co = checkout
	st = status
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	update = !sh (git diff --exit-code > devnull && git fetch && git reset --hard @{u} && echo Branch Updated ) || (echo Repo is Dirty, Update canceled!)
[core]
	editor = nano
[core]
	excludesfile = ~/.gitignore_global
[pull]
	ff = only

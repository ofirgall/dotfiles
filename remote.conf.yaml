- shell:
    - [git submodule update --init --recursive, Installing submodules]

- defaults:
    link:
      relink: true
      force: true
      create: true

- link:
    ~/.zshrc:
        path: dotfiles/zshrc
    ~/.zsh_conf:
        path: dotfiles/zsh_conf
    ~/.profile:
        path: dotfiles/profile
    ~/.gitconfig:
        path: dotfiles/gitconfig
    ~/.gitignore_global:
        path: dotfiles/gitignore_global
    ~/.tmux.conf:
        path: dotfiles/tmux.conf
    ~/.tmux_conf:
        path: dotfiles/tmux_conf
    ~/.tmux/custom-macros:
        path: dotfiles/tmux_conf/custom-macros
    ~/dotfiles_scripts:
        path: scripts
    ~/.config/nvim:
        path: editors/nvim
    ~/.vimrc:
        path: dotfiles/vimrc

- create:
    - ~/projects
    - ~/workspace

- shell:
    - [install_scripts/install_zsh.sh, Installing ZSH]
    - [install_scripts/install_basic.sh, Running basic libs installation]
    - [install_scripts/install_lsp.sh, Installing LSP Utils]
    - [install_scripts/install_nvim.sh, Installing Neovim]
    - [install_scripts/install_tmux.sh, Installing tmux]
